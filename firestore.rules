rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Require authentication for all read and write operations.
      allow read, write: if request.auth != null;
    }
  }
}
