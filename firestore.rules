rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ✅ SECURE: Require authentication for all read/write operations
      allow read, write: if request.auth != null;
    }
  }
}
